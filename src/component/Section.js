import React from 'react'

const Section = ({name, color = 'black'}) => {
  const fColorStyles = {
    color: color,
    fontSize:'24px',
    fontWeight:'bold',
  };
  return (
    <div className='Section'>
      
    </div>
  )
}

export default Section